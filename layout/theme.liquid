<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ page_title }} | {{ shop.name }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {{ content_for_header }}
  </head>
  <body>

    <header>
      <h1>{{ shop.name }}</h1>
     
    </header>

    <main>
      {{ content_for_layout }}
    </main>

   
  </body>
</html>
