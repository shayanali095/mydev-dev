{%- assign hero_height = section.settings.hero_height -%}


<section class="custom-hero" 
  style="
    background-image: url({{ section.settings.hero_background | image_url: width: 1600 }});
    background-size: cover;
    background-position: center;
    height: {{ hero_height | default: 270}}px;
    color: {{ section.settings.text_color }};
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 0 20px;">
    
  <div class="hero-content" style="max-width: 800px; width: 100%;">
    {% if section.settings.hero_title != blank %}
      <h1 style="font-size: 2.5rem; margin-bottom: 1rem;">{{ section.settings.hero_title }}</h1>
    {% endif %}
    
    {% if section.settings.hero_subtitle != blank %}
      <p style="font-size: 1.25rem; margin-bottom: 1.5rem;">{{ section.settings.hero_subtitle }}</p>
    {% endif %}
    
    {% if section.settings.hero_button_label != blank %}
      <a href="{{ section.settings.hero_button_link }}" class="hero-button"
         style="background: {{ section.settings.button_color }}; color: {{ section.settings.button_text_color }}; padding: 14px 28px; text-decoration: none; font-weight: bold; border-radius: 6px;">
        {{ section.settings.hero_button_label }}
      </a>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Custom Hero",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_background",
      "label": "Background Image"
    },
    {
      "type": "text",
      "id": "hero_title",
      "label": "Hero Title",
      "default": "Welcome to Our Store"
    },
    {
      "type": "text",
      "id": "hero_subtitle",
      "label": "Hero Subtitle",
      "default": "Discover our latest collection and best deals"
    },
    {
      "type": "text",
      "id": "hero_button_label",
      "label": "Button Label",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "hero_button_link",
      "label": "Button Link"
    },
    {
      "type": "range",
      "id": "hero_height",
      "label": "Hero Section Height (in px)",
      "min": 50,
      "max": 600,
      "step": 10,
      "default": 270
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button Background Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#ffffff"
    }
  ]
}
{% endschema %}
